"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveContainer = void 0;
const resolveContainer = (tokens, idx) => {
    const { type, info } = tokens[idx];
    const [customType, title] = info.trim().split(' ');
    if (/container\_\w+\_open/g.test(type)) {
        return `<div class="custom-container ${customType}"><p class="custom-container-title">${title || customType.toUpperCase()}</p>`;
    }
    if (/container\_\w+\_close/g.test(type)) {
        return '</div>';
    }
};
exports.resolveContainer = resolveContainer;
